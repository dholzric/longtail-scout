node_modules/
.wrangler/
dist/
.dev.vars
.env
.env.local
*.log
.DS_Store
coverage/
demand-snapshot.json
NUL
.playwright-mcp/
*-snapshot.png
*-overlay.png
api-docs.png
NOTES_*.md

# Saved lablab submission page (local working artifact, not project source)
submit.page/

# API keys / secrets — never commit (repo is public)
secrets/
*.key
.secrets*
