{
  "name": "longtail-scout",
  "version": "1.7.3",
  "private": true,
  "scripts": {
    "build:web": "pnpm -C web build",
    "deploy": "pnpm build:web && pnpm -C worker run deploy",
    "dev:worker": "pnpm -C worker dev",
    "dev:web": "pnpm -C web dev"
  }
}
