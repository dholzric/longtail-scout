/**
 * App (frontend) version — shown in the footer. Keep in lockstep with worker/src/version.ts;
 * the worker exposes the authoritative value at /api/health. See that file for the bump scheme.
 */
export const APP_VERSION = "1.8.3";
